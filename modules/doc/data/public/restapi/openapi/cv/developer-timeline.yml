get:
  tags:
    - CV
  summary: Developer timeline
  parameters:
    - in: query
      name: reverse
      schema:
        type: boolean
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "./schemas.yml#/schemas/DevTimelineItem"
