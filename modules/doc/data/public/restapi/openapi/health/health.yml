get:
  tags:
    - Health
  summary: Health status
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: "./schemas.yml#/schemas/HealthResponse"
